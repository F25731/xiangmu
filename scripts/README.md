Put your transfer scripts here when deploying with Docker:

- quark_xinyue_test.py
- baidu_openlist_test.py

The default paths in the control page are:

- /app/scripts/quark_xinyue_test.py
- /app/scripts/baidu_openlist_test.py

You can also change the script paths in the database/config UI if you mount them elsewhere.
